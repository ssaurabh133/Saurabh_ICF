/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.masters.actions;

import java.util.ArrayList;
import java.util.Properties;

import javax.naming.InitialContext;
import javax.servlet.ServletContext;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.log4j.Logger;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.apache.struts.actions.DispatchAction;
import org.apache.struts.validator.DynaValidatorForm;

import com.business.ejbClient.CreditProcessingMasterBussinessSessionBeanRemote;
import com.commonFunction.dao.commonDao;
import com.connect.CommonFunction;
import com.connect.LookUpInstanceFactory;
import com.logger.LoggerMsg;
import com.login.dao.UserSessionCheck;
import com.login.roleManager.UserObject;
import com.masters.vo.GoldOrnamentMasterVo;
import com.masters.vo.ProductMasterVo;


public class GoldOrnamentMasterDispatchAction extends DispatchAction {
	private static final Logger logger = Logger.getLogger(GoldOrnamentMasterDispatchAction.class.getName());
	
	public ActionForward openAddGoldOrnament(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response)throws Exception {
		ServletContext context = getServlet().getServletContext();
			LoggerMsg.info("in GoldOrnament openAddGoldOrnament()");
			HttpSession session = request.getSession();
			boolean flag=false;
			UserObject userobj=(UserObject)session.getAttribute("userobject");
			Object sessionId = session.getAttribute("sessionID");
			//for check User session start
			String strFlag="";	
			if(sessionId!=null)
			{
				strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
			}
			
			logger.info("strFlag .............. "+strFlag);
			if(!strFlag.equalsIgnoreCase(""))
			{
				if(strFlag.equalsIgnoreCase("sameUserSession"))
				{
					context.removeAttribute("msg");
					context.removeAttribute("msg1");
				}
				else if(strFlag.equalsIgnoreCase("BODCheck"))
				{
					context.setAttribute("msg", "B");
				}
				return mapping.findForward("logout");
			}

			CreditProcessingMasterBussinessSessionBeanRemote cpm = (CreditProcessingMasterBussinessSessionBeanRemote) LookUpInstanceFactory.getLookUpInstance(CreditProcessingMasterBussinessSessionBeanRemote.REMOTE_IDENTITY, request);
			
			ArrayList list = cpm.getGoldOrnamentCategory();
			request.setAttribute("goldOrnamentCategor", list);
			request.setAttribute("save", "save");
			return mapping.findForward("openAdd");
	}
	

	
	public ActionForward saveGoldOrnamentDetails(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse resopnse) throws Exception{
		ServletContext context = getServlet().getServletContext();
		HttpSession session=request.getSession(false);
		UserObject userobj=new UserObject();
		userobj=(UserObject)session.getAttribute("userobject");
		String userId="";
		String bDate="";
		if(userobj!=null)
		{
				userId=userobj.getUserId();
				bDate=userobj.getBusinessdate();
		}
		
		boolean flag=false;
		
		Object sessionId = session.getAttribute("sessionID");
		//for check User session start
		String strFlag="";	
		if(sessionId!=null)
		{
			strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
		}
		
		logger.info("strFlag .............. "+strFlag);
		if(!strFlag.equalsIgnoreCase(""))
		{
			if(strFlag.equalsIgnoreCase("sameUserSession"))
			{
				context.removeAttribute("msg");
				context.removeAttribute("msg1");
			}
			else if(strFlag.equalsIgnoreCase("BODCheck"))
			{
				context.setAttribute("msg", "B");
			}
			return mapping.findForward("logout");
		}
		DynaValidatorForm GoldOrnamentMasterAddDyanavalidatiorForm= (DynaValidatorForm)form;
		GoldOrnamentMasterVo vo = new GoldOrnamentMasterVo();
		org.apache.commons.beanutils.BeanUtils.copyProperties(vo, GoldOrnamentMasterAddDyanavalidatiorForm);	
		
		vo.setMakerId(userId);
		vo.setMakerDate(bDate);
		
		CreditProcessingMasterBussinessSessionBeanRemote cpm = (CreditProcessingMasterBussinessSessionBeanRemote) LookUpInstanceFactory.getLookUpInstance(CreditProcessingMasterBussinessSessionBeanRemote.REMOTE_IDENTITY, request);
		
		String sms="";
	
		String status = cpm.insertGoldOrnamentMaster(vo);
		logger.info("Inside Gold Ornament Master Action.....displaying status...."+status);
		if(CommonFunction.checkNull(status).equalsIgnoreCase("S")){
			sms=status;
			request.setAttribute("sms",sms);
			request.setAttribute("save", "save");
		}
		else{
			sms=status;
			request.setAttribute("sms",sms);
			ArrayList list = cpm.getGoldOrnamentCategory();
			request.setAttribute("GoldOrnamentCategory", list);
			request.setAttribute("save", "save");
		}
		logger.info("status"+status);
		return mapping.getInputForward();
	}
	
		
	
		public ActionForward openEditgoldOrnament(ActionMapping mapping, ActionForm form,
				HttpServletRequest request, HttpServletResponse response)	throws Exception {
			ServletContext context = getServlet().getServletContext();
			GoldOrnamentMasterVo goldMasterVo=new GoldOrnamentMasterVo(); 
							logger.info("In openEditGoldOrnament");
							HttpSession session = request.getSession();
							boolean flag=false;
							UserObject userobj=(UserObject)session.getAttribute("userobject");
							Object sessionId = session.getAttribute("sessionID");
							//for check User session start
							String strFlag="";	
							if(sessionId!=null)
							{
								strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
							}
							
							logger.info("strFlag .............. "+strFlag);
							if(!strFlag.equalsIgnoreCase(""))
							{
								if(strFlag.equalsIgnoreCase("sameUserSession"))
								{
									context.removeAttribute("msg");
									context.removeAttribute("msg1");
								}
								else if(strFlag.equalsIgnoreCase("BODCheck"))
								{
									context.setAttribute("msg", "B");
								}
								return mapping.findForward("logout");
							}
							goldMasterVo.setOrnamentId(request.getParameter("ornamentId"));
							goldMasterVo.setGoldORnamentStatus(request.getParameter("goldORnamentStatus"));
							CreditProcessingMasterBussinessSessionBeanRemote cpm = (CreditProcessingMasterBussinessSessionBeanRemote) LookUpInstanceFactory.getLookUpInstance(CreditProcessingMasterBussinessSessionBeanRemote.REMOTE_IDENTITY, request);
							
					        
							ArrayList<GoldOrnamentMasterVo> list = cpm.searchGoldOrnamentData(goldMasterVo);
							logger.info("In openEditGold GoldMasterVo list"+list.size());
							request.setAttribute("list", list);
						
							GoldOrnamentMasterVo docVo=new GoldOrnamentMasterVo();
							docVo=(GoldOrnamentMasterVo)list.get(0);
						
							request.setAttribute("ornamentId", docVo.getOrnamentId());
							request.setAttribute("status", docVo.getGoldORnamentStatus());
							//request.setAttribute("assetFlag", goldMasterVo.getAssetFlag());
				
							
							request.setAttribute("editVal", "editVal");
						   return mapping.findForward("editGoldOrnament");	
						}
				
				
				public ActionForward updateGoldOrnament(ActionMapping mapping, ActionForm form,
						HttpServletRequest request, HttpServletResponse response) throws Exception {
					ServletContext context = getServlet().getServletContext();
					logger.info("In updateGoldMaster.......");
					HttpSession session = request.getSession();
					boolean flag=false;
					UserObject userobj=(UserObject)session.getAttribute("userobject");
					Object sessionId = session.getAttribute("sessionID");
					//for check User session start
					String strFlag="";	
					if(sessionId!=null)
					{
						strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
					}
					
					logger.info("strFlag .............. "+strFlag);
					if(!strFlag.equalsIgnoreCase(""))
					{
						if(strFlag.equalsIgnoreCase("sameUserSession"))
						{
							context.removeAttribute("msg");
							context.removeAttribute("msg1");
						}
						else if(strFlag.equalsIgnoreCase("BODCheck"))
						{
							context.setAttribute("msg", "B");
						}
						return mapping.findForward("logout");
					}
					GoldOrnamentMasterVo vo=new GoldOrnamentMasterVo(); 
					DynaValidatorForm GoldOrnamentMasterAddDyanavalidatiorForm= (DynaValidatorForm)form;
					org.apache.commons.beanutils.BeanUtils.copyProperties(vo, GoldOrnamentMasterAddDyanavalidatiorForm);	

					logger.info("In updateGoldOrnamentDetails---status by form- OrnamentId----"+GoldOrnamentMasterAddDyanavalidatiorForm.getString("ornamentId"));  
					vo.setGoldORnamentStatus(request.getParameter("goldORnamentStatus"));
					 
					vo.setOrnamentId(request.getParameter("ornamentId"));
					vo.setGoldOrnament(request.getParameter("goldOrnament"));
					//vo.setGoldORnamentStatus(request.getParameter("goldORnamentStatus"));

					CreditProcessingMasterBussinessSessionBeanRemote cpm = (CreditProcessingMasterBussinessSessionBeanRemote) LookUpInstanceFactory.getLookUpInstance(CreditProcessingMasterBussinessSessionBeanRemote.REMOTE_IDENTITY, request);
					
			        boolean status=cpm.updateGoldOrnamentData(vo);
			        String sms="";
			        if(status){
			        	sms="M";
						request.setAttribute("sms",sms);
						request.setAttribute("editValUpdate", "editValUpdate");
						
					}
					else{
						sms="E";
						request.setAttribute("sms",sms);
						ArrayList<GoldOrnamentMasterVo> list =new ArrayList<GoldOrnamentMasterVo>();
						list.add(vo);
						ArrayList list1 = cpm.getGoldOrnamentCategory();
						request.setAttribute("getGoldOrnamentCategory", list1);
						logger.info("In openEditgoldOrnament list"+ list.size());
						
						request.setAttribute("editValUpdate", "editValUpdate");
						request.setAttribute("list", list);
						//request.setAttribute("goldOrnamentId", vo.getOrnamentId());
					}
			        	                      
			        return mapping.getInputForward();
			      
					
				}
				
				
				
				
	
}