/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.cp.actions;
import javax.servlet.ServletContext;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;
import org.apache.log4j.Logger;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.apache.struts.actions.DispatchAction;
import org.apache.struts.validator.DynaValidatorForm;
import com.cp.dao.CreditProcessingDAO;
import com.cp.vo.ChargeVo;
import com.login.dao.UserSessionCheck;
import com.login.roleManager.UserObject;
import com.connect.DaoImplInstanceFactory;

/**
 * MyEclipse Struts Creation date: 05-05-2011
 * 
 * XDoclet definition:
 * 
 * @struts.action validate="true"
 */
public class ChargesProcessActionForEmiCalc extends DispatchAction {
	private static final Logger logger = Logger.getLogger(ChargesProcessActionForEmiCalc.class.getName());
	/*
	 * Generated Methods
	 */

	/**
	 * Method execute
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response)
			throws Exception {
		
		logger.info("In ChargesProcessActionForEmiCalc(execute) ");

		HttpSession session = request.getSession();

		boolean flag=false;
		UserObject userobj=(UserObject)session.getAttribute("userobject");
		String userId="";
		String bDate="";
		
		if(userobj!=null)
		{		userId=userobj.getUserId();
				bDate=userobj.getBusinessdate();				
		}else{
			logger.info("here in execute  method of ChargesProcessActionForEmiCalc action the session is out----------------");
			return mapping.findForward("sessionOut");
		}
		DynaValidatorForm ChargesDynaValidatorForm = (DynaValidatorForm) form;
		ChargeVo vo = new ChargeVo();
		org.apache.commons.beanutils.BeanUtils.copyProperties(vo,ChargesDynaValidatorForm);
		Object sessionId = session.getAttribute("sessionID");
		//for check User session start
		ServletContext context = getServlet().getServletContext();
		String strFlag="";	
		if(sessionId!=null)
		{
			strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
		}
		
		logger.info("strFlag .............. "+strFlag);
		if(!strFlag.equalsIgnoreCase(""))
		{
			if(strFlag.equalsIgnoreCase("sameUserSession"))
			{
				context.removeAttribute("msg");
				context.removeAttribute("msg1");
			}
			else if(strFlag.equalsIgnoreCase("BODCheck"))
			{
				context.setAttribute("msg", "B");
			}
			return mapping.findForward("logout");
		}

		String dealId = "";

		if (session.getAttribute("dealId") != null) {

			dealId = session.getAttribute("dealId").toString();
		} 
		logger.info("In ChargesProcessActionForEmiCalc dealId id: " + dealId);

	
		vo.setUserId(userId);
		vo.setDealId(dealId);
		vo.setBussinessDate(bDate);
		
			//CreditProcessingDAO detail = new CreditProcessingDAOImpl();
            CreditProcessingDAO detail=(CreditProcessingDAO)DaoImplInstanceFactory.getDaoImplInstance(CreditProcessingDAO.IDENTITY);
            logger.info("Implementation class: "+detail.getClass()); 		//changed by anil              
			boolean status = detail.saveChargesForEmiCal(vo);
			String sms = "";
			if (status) {
				
				sms = "S";
				
				boolean status1 = detail.updatesecuritydepositForEmiCalc(vo,dealId);
				logger.info("status for table cr_deal_txncharges_dtl_emi_calc"+status1);
				
			} else {
				sms = "E";
			}
			request.setAttribute("sms", sms);
			logger.info("In ChargesProcessActionForEmiCalc(execute) status: " + status);
			return mapping.findForward("success");
		}

}