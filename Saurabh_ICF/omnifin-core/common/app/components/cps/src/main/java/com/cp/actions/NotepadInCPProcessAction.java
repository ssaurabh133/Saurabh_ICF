/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.cp.actions;

import java.util.ArrayList;
import javax.servlet.ServletContext;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;
import org.apache.log4j.Logger;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.apache.struts.actions.DispatchAction;
import org.apache.struts.validator.DynaValidatorForm;
import com.cp.dao.NotepadDAO;
import com.connect.CommonFunction;
import com.connect.DaoImplInstanceFactory;
import com.cp.dao.CreditProcessingDAO;
import com.cp.vo.CreditProcessingNotepadVo;
import com.login.dao.UserSessionCheck;
import com.login.roleManager.UserObject;

/** 
 * MyEclipse Struts
 * Creation date: 12-26-2011
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class NotepadInCPProcessAction extends DispatchAction {
	/*
	 * Generated Methods
	 */
	private static final Logger logger = Logger.getLogger(NotepadInCPProcessAction.class.getName());
	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward saveNotepadDataInCP(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response)throws Exception {


		logger.info("In NotepadInCPProcessAction(execute)");
		HttpSession session = request.getSession();
		boolean flag=false;
		String userId="";
		String bDate="";
		String dealId="";
		UserObject userobj=(UserObject)session.getAttribute("userobject");
		if(userobj!=null)
		{
			userId = userobj.getUserId();
			bDate=userobj.getBusinessdate();
		}else{
			logger.info("here in saveNotepadDataInCP method of  NotepadInCMProcessAction action the session is out----------------");
			return mapping.findForward("sessionOut");
		}
		Object sessionId = session.getAttribute("sessionID");
		//for check User session start
		ServletContext context = getServlet().getServletContext();
		String strFlag="";	
		if(sessionId!=null)
		{
			strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
		}
		
		logger.info("strFlag .............. "+strFlag);
		if(!strFlag.equalsIgnoreCase(""))
		{
			if(strFlag.equalsIgnoreCase("sameUserSession"))
			{
				context.removeAttribute("msg");
				context.removeAttribute("msg1");
			}
			else if(strFlag.equalsIgnoreCase("BODCheck"))
			{
				context.setAttribute("msg", "B");
			}
			return mapping.findForward("logout");
		}
		DynaValidatorForm CreditProcessingNotepadDynaValidatorActionForm = (DynaValidatorForm) form;


		if(session.getAttribute("dealId")!=null && !CommonFunction.checkNull(session.getAttribute("dealId")).equalsIgnoreCase(""))
		{
			dealId=	session.getAttribute("dealId").toString();
		}
		logger.info("dealId: " +dealId);
		CreditProcessingNotepadVo cr = new CreditProcessingNotepadVo();
		cr.setUserId("" + userId);
		cr.setMakerDate(bDate);
		org.apache.commons.beanutils.BeanUtils.copyProperties(cr,CreditProcessingNotepadDynaValidatorActionForm);
		cr.setDealId(dealId);
	//	cr.setLoanId(dealId);
		CreditProcessingDAO service=(CreditProcessingDAO)DaoImplInstanceFactory.getDaoImplInstance(CreditProcessingDAO.IDENTITY);
        logger.info("Implementation class: "+service.getClass()); 
		//CreditProcessingDAO service = new CreditProcessingDAOImpl();
        NotepadDAO dao=(NotepadDAO)DaoImplInstanceFactory.getDaoImplInstance(NotepadDAO.IDENTITY);
		logger.info("Implementation class: "+dao.getClass()); 
		logger.info("cr.getDealId : " + cr.getDealId());
		logger.info("cr.getLoanId : " + cr.getLoanId());
		logger.info("cr.getFollowUp : " + cr.getFollowUp());
		logger.info("cr.getLoanId : " + cr.getLoanId());
		boolean status = service.saveNotepadData(cr);
		logger.info("Nis Status : " + status);
		if(status){
			request.setAttribute("sms",'S');
		}else{
			request.setAttribute("sms",'E');
		}
		//if (dealId != null && !dealId.equalsIgnoreCase("")) {
			ArrayList list = service.getNotepadData(dealId,"DC");
			//ArrayList listInloan = service.getNotepadDataInLoanInit(dealId,"DC");
			//request.setAttribute("listInloan", listInloan);
			request.setAttribute("list", list);
			ArrayList leadList = service.getLeadNotepadData(dealId,"LT");
			request.setAttribute("leadList", leadList);
			ArrayList dealHeader = dao.getDealHeader(dealId);
			request.setAttribute("dealHeader", dealHeader);
			ArrayList noteCode = service.getNoteCode();
			request.setAttribute("noteCode", noteCode);
		//} 
		return mapping.findForward("success");
	}
}