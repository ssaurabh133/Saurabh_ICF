/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.legal.actions;

import java.util.ArrayList;
import java.util.Properties;

import javax.naming.InitialContext;
import javax.servlet.ServletContext;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.log4j.Logger;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.apache.struts.actions.DispatchAction;
import org.apache.struts.validator.DynaValidatorForm;
import com.business.ejbClient.CommonMasterBussinessSessionBeanRemote;
import com.business.legalMasterBussiness.LegalMasterBusinessSessionBeanRemote;
import com.business.legalTransactionBussiness.LegalTransactionBusinessSessionBeanRemote;
import com.connect.CommonFunction;
import com.legal.vo.CaseTypeMasterVo;
import com.legal.vo.LegalNoticeInitiationVo;
import com.login.dao.UserSessionCheck;
import com.login.roleManager.UserObject;
import com.masters.vo.CountryMasterVo;

public class LegalNoticeInitiationMakerDispatchAction extends DispatchAction {
	private static final Logger logger = Logger.getLogger(LegalNoticeInitiationMakerDispatchAction.class.getName());
	public ActionForward openAddLegalNoticeInitiationMaker(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response)	throws Exception
			{
				logger.info(" in openAddCaseType()");
				ServletContext context = getServlet().getServletContext();
				HttpSession session = request.getSession();
				boolean flag=false;
				UserObject userobj=(UserObject)session.getAttribute("userobject");
				Object sessionId = session.getAttribute("sessionID");
				//for check User session start
				String strFlag="";
				if(sessionId!=null)
				{
					strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
				}
				
				logger.info("strFlag .............. "+strFlag);
				if(!strFlag.equalsIgnoreCase(""))
				{
					if(strFlag.equalsIgnoreCase("sameUserSession"))
					{
						context.removeAttribute("msg");
						context.removeAttribute("msg1");
					}
					else if(strFlag.equalsIgnoreCase("BODCheck"))
					{
						context.setAttribute("msg", "B");
					}
					return mapping.findForward("logout");
				}
				
				request.setAttribute("save", "save");
			    return mapping.findForward("openAdd");	
			}
	
	
	public ActionForward saveLegalNoticeInitiationMaker(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse resopnse) throws Exception{
			ServletContext context = getServlet().getServletContext();
			//HttpSession session=request.getSession(false);
			HttpSession session = request.getSession();
			boolean flag=false;
			UserObject userobj=(UserObject)session.getAttribute("userobject");
			Object sessionId = session.getAttribute("sessionID");
			//for check User session start
			String strFlag="";
			if(sessionId!=null)
			{
				strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
			}
			
			logger.info("strFlag .............. "+strFlag);
			if(!strFlag.equalsIgnoreCase(""))
			{
				if(strFlag.equalsIgnoreCase("sameUserSession"))
				{
					context.removeAttribute("msg");
					context.removeAttribute("msg1");
				}
				else if(strFlag.equalsIgnoreCase("BODCheck"))
				{
					context.setAttribute("msg", "B");
				}
				return mapping.findForward("logout");
			}
			
			String userId="";
			String bDate="";
			if(userobj!=null)
			{
					userId=userobj.getUserId();
					bDate=userobj.getBusinessdate();
			}
	
		DynaValidatorForm dynaForm= (DynaValidatorForm)form;
		LegalNoticeInitiationVo vo = new LegalNoticeInitiationVo();
		org.apache.commons.beanutils.BeanUtils.copyProperties(vo, dynaForm);
		

		vo.setMakerId(userId);
		vo.setMakerDate(bDate);
		
		Properties props = new Properties();
		props.load(request.getSession().getServletContext().getResourceAsStream("/WEB-INF/jndi.properties"));   
        InitialContext ic = new InitialContext(props);
        String applName=props.getProperty("enterprise.application.name");
        String remoteBean = props.getProperty("legal.transaction.business.session.bean.remote");
        logger.info("remoteBean : "+remoteBean);
        logger.info("applName : "+applName);
        String remoteBeanName=CommonFunction.checkNull(applName)+CommonFunction.checkNull(remoteBean);
        logger.info("remoteBeanName: "+remoteBeanName);
        LegalTransactionBusinessSessionBeanRemote bp=(LegalTransactionBusinessSessionBeanRemote)ic.lookup(remoteBeanName);
	
		String sms="";
		
		String updateForwardFlag = CommonFunction.checkNull(request.getParameter("updateForwardFlag"));
        
        logger.info("## In updateLegalNoticeInitiationMaker() : updateForwardFlag :==>> "+updateForwardFlag);
        
        vo.setUpdateForwardFlag(updateForwardFlag);
        
        logger.info("## In updateLegalNoticeInitiationMaker() : vo.getUpdateForwardFlag() :==>> "+vo.getUpdateForwardFlag());
	
		boolean status = bp.insertLegalNoticeInitiationMakerData(vo);
		logger.info("Inside Country Master Action.....displaying status...."+status);
		if(status){
			
			if(vo.getUpdateForwardFlag().equalsIgnoreCase("S"))
			{
				sms="S";
			      String noticeId = bp.getNoticeId();
			              logger.info("value of noticeId is ::  " + noticeId);
			              request.setAttribute("noticeId", noticeId);
			}
			else if(vo.getUpdateForwardFlag().equalsIgnoreCase("F"))
        	{
				sms="F";
        	}
			
		}
		else{
			
			logger.info("## In saveLegalNoticeInitiationMaker () :vo.getNoticeMessage() : ==>> "+CommonFunction.checkNull(vo.getNoticeMessage()));
			
			if(!CommonFunction.checkNull(vo.getNoticeMessage()).equalsIgnoreCase(""))
			{
				
				logger.info("## In saveLegalNoticeInitiationMaker () : for notice 138 lrn must sent: ");
				
				sms="N";
				
				
				
				request.setAttribute("noticemsg", vo.getNoticeMessage());
				
			}
			else
			{
				sms="E";
				
			}
			
			ArrayList<LegalNoticeInitiationVo> list =new ArrayList<LegalNoticeInitiationVo>();
			list.add(vo);
			logger.info("In update case detail list"+ list.size());
			
			request.setAttribute("save", "save");
		}
		
		request.setAttribute("sms",sms);
		
		logger.info("status"+status);
		return mapping.getInputForward();
	}
	
	
	public ActionForward openEditLegalNoticeInitiationMaker(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response)	throws Exception { 
		ServletContext context = getServlet().getServletContext();
						LegalNoticeInitiationVo vo=new LegalNoticeInitiationVo(); 
						logger.info("In openEditCaseTypeMaster");
						
						HttpSession session = request.getSession();
						boolean flag=false;
						UserObject userobj=(UserObject)session.getAttribute("userobject");
						Object sessionId = session.getAttribute("sessionID");
						//for check User session start
						String strFlag="";
						if(sessionId!=null)
						{
							strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
						}
						
						logger.info("strFlag .............. "+strFlag);
						if(!strFlag.equalsIgnoreCase(""))
						{
							if(strFlag.equalsIgnoreCase("sameUserSession"))
							{
								context.removeAttribute("msg");
								context.removeAttribute("msg1");
							}
							else if(strFlag.equalsIgnoreCase("BODCheck"))
							{
								context.setAttribute("msg", "B");
							}
							return mapping.findForward("logout");
						}
						
						vo.setNoticeId(request.getParameter("noticeId"));
						logger.info("In openEditCaseTypeMaster---status---- by getpara-"+request.getParameter("noticeId"));  
						logger.info("In openEditCaseTypeMaster---status---- by getpara by vo-"+vo.getNoticeId());
						
						Properties props = new Properties();
						props.load(request.getSession().getServletContext().getResourceAsStream("/WEB-INF/jndi.properties"));   
				        InitialContext ic = new InitialContext(props);
				        String applName=props.getProperty("enterprise.application.name");
				        String remoteBean = props.getProperty("legal.transaction.business.session.bean.remote");
				        logger.info("remoteBean : "+remoteBean);
				        logger.info("applName : "+applName);
				        String remoteBeanName=CommonFunction.checkNull(applName)+CommonFunction.checkNull(remoteBean);
				        logger.info("remoteBeanName: "+remoteBeanName);
				        LegalTransactionBusinessSessionBeanRemote bp=(LegalTransactionBusinessSessionBeanRemote)ic.lookup(remoteBeanName);
										
						ArrayList list = bp.editLegalNoticeInitiationMakerData(vo);
						logger.info("In openEditCaseTypeMaster LegalNoticeInitiationVo list"+list.size());
						request.setAttribute("list", list);
					
						
						vo=(LegalNoticeInitiationVo) list.get(0);
						logger.info("vo.getModeOfNotice() : ==>> "+vo.getModeOfNotice());
						
						LegalNoticeInitiationVo docVo=new LegalNoticeInitiationVo();
						docVo=(LegalNoticeInitiationVo) list.get(0);
						logger.info("In openEditCaseTypeMaster---status---- by getpara by vo-"+docVo.getRecStatus());
						
					
						request.setAttribute("status", vo.getRecStatus());
						request.setAttribute("editVal", "editVal");
					   return mapping.findForward("editLegalNoticeInitiationMaker");	
		}
	
	
	public ActionForward updateLegalNoticeInitiationMaker(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) throws Exception {
		ServletContext context = getServlet().getServletContext();
		logger.info("In updateCase.......");
		HttpSession session = request.getSession();
		boolean flag=false;
		UserObject userobj=(UserObject)session.getAttribute("userobject");
		Object sessionId = session.getAttribute("sessionID");
		//for check User session start
		String strFlag="";
		if(sessionId!=null)
		{
			strFlag = UserSessionCheck.checkSameUserSession(userobj,sessionId.toString(),"",request);
		}
		
		logger.info("strFlag .............. "+strFlag);
		if(!strFlag.equalsIgnoreCase(""))
		{
			if(strFlag.equalsIgnoreCase("sameUserSession"))
			{
				context.removeAttribute("msg");
				context.removeAttribute("msg1");
			}
			else if(strFlag.equalsIgnoreCase("BODCheck"))
			{
				context.setAttribute("msg", "B");
			}
			return mapping.findForward("logout");
		}
		
		String userId="";
		String bDate="";
		if(userobj!=null)
		{
				userId=userobj.getUserId();
				bDate=userobj.getBusinessdate();
		}
		
		LegalNoticeInitiationVo vo=new LegalNoticeInitiationVo(); 
		DynaValidatorForm dynaForm= (DynaValidatorForm)form;
		org.apache.commons.beanutils.BeanUtils.copyProperties(vo, dynaForm);	

		logger.info("In updateCountryDetails---------");  
		
		vo.setMakerId(userId);
		vo.setMakerDate(bDate);
		
		Properties props = new Properties();
		props.load(request.getSession().getServletContext().getResourceAsStream("/WEB-INF/jndi.properties"));   
        InitialContext ic = new InitialContext(props);
        String applName=props.getProperty("enterprise.application.name");
        String remoteBean = props.getProperty("legal.transaction.business.session.bean.remote");
        logger.info("remoteBean : "+remoteBean);
        logger.info("applName : "+applName);
        String remoteBeanName=CommonFunction.checkNull(applName)+CommonFunction.checkNull(remoteBean);
        logger.info("remoteBeanName: "+remoteBeanName);
        LegalTransactionBusinessSessionBeanRemote bp=(LegalTransactionBusinessSessionBeanRemote)ic.lookup(remoteBeanName);
        
        String updateForwardFlag = CommonFunction.checkNull(request.getParameter("updateForwardFlag"));
        
        logger.info("## In updateLegalNoticeInitiationMaker() : updateForwardFlag :==>> "+updateForwardFlag);
        
        vo.setUpdateForwardFlag(updateForwardFlag);
        
        logger.info("## In updateLegalNoticeInitiationMaker() : vo.getUpdateForwardFlag() :==>> "+vo.getUpdateForwardFlag());
		
        boolean status=bp.updateLegalNoticeInitiationMakerData(vo);
        
        logger.info("## In updateLegalNoticeInitiationMaker() : status : ==>> "+status);
        logger.info("## In updateLegalNoticeInitiationMaker() : vo.getNoticeId() : ==>> "+vo.getNoticeId());
        
        String sms="";
        if(status){
        	
        	if(vo.getUpdateForwardFlag().equalsIgnoreCase("U"))
        	{
			sms="M";
			request.setAttribute("sms",sms);
			request.setAttribute("editVal", "editVal");
			request.setAttribute("noticeId", vo.getNoticeId());
        	}
        	else if(vo.getUpdateForwardFlag().equalsIgnoreCase("F"))
        	{
        	sms="F";
			request.setAttribute("sms",sms);
			request.setAttribute("editVal", "editVal");
        	}
		
		}
		else{
			
			logger.info("## In updateLegalNoticeInitiationMaker() : vo.getNoticeMessage() : "+vo.getNoticeMessage());
			
			if(!CommonFunction.checkNull(vo.getNoticeMessage()).equalsIgnoreCase(""))
			{
				logger.info("## In updateLegalNoticeInitiationMaker() :before notice138 notice lrn must sent");
				sms="N";
				
				request.setAttribute("noticemsg", vo.getNoticeMessage());
				
			}
			else
			{
				sms="E";
				
			}
			
			request.setAttribute("sms",sms);
			ArrayList<LegalNoticeInitiationVo> list =new ArrayList<LegalNoticeInitiationVo>();
			list.add(vo);
			logger.info("In update case detail list"+ list.size());
			
			request.setAttribute("editVal", "editVal");
			request.setAttribute("list", list);
			request.setAttribute("status", vo.getRecStatus());
			
		}
       // vo.setCountryId(request.getParameter("CountryId"));
		logger.info("In update Case Detail---status-----"+vo.getRecStatus());

		        
        return mapping.getInputForward();
      
		
	}
				
}